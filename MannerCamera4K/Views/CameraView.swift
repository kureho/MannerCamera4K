import SwiftUI

struct CameraView: View {
    var body: some View {
        ZStack {
            Color.black.ignoresSafeArea()
            Text("マナーカメラ 4K")
                .foregroundStyle(.white)
        }
    }
}
